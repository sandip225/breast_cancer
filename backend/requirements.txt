# FastAPI Backend Dependencies (older versions fully compatible with TensorFlow 2.13)
fastapi==0.95.2
uvicorn[standard]==0.22.0
python-multipart==0.0.6
typing-extensions==4.5.0
pydantic==1.10.9

# Image Processing (memory optimized)
pillow==10.4.0
numpy==1.24.3
opencv-python-headless==4.8.0.76
# pytesseract removed - not essential for core functionality

# Visualization & Reports (optimized versions)
matplotlib==3.7.1
scipy==1.10.1
reportlab==4.0.0

# Machine Learning - Optimized for Render free tier (512MB RAM)
# Using minimal TensorFlow version
tensorflow-cpu==2.13.0

# PyTorch removed to save memory - not critical for deployment
# torch>=2.0.0
# torchvision>=0.15.0
# timm>=0.9.0

# Note: Model file must be in repository (use Git LFS for large files)
